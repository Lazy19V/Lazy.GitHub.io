~ $ git clone 
git clone https://github.com/lazy/lazy.github.io
~ $ cd lazy.github.io
~ $ echo "Hello World" > index.html
~ $ git add --all
~ $ git commit -m "Initial commit"
~ $ git push -u origin main
